/* Aprende IA: añadir módulos aquí permite escalar el curso sin cambiar la interfaz. */
const modules = [
  { id: 'ia-basica', title: '¿Qué es la IA?', description: 'Una primera exploración práctica de qué puede y no puede hacer.', icon: '🧠', available: true },
  { id: 'conversar', title: 'Cómo hablar con una IA', description: 'Da instrucciones claras y obtén mejores resultados.', icon: '💬' },
  { id: 'prompts', title: 'El arte de crear buenos prompts', description: 'Convierte una idea vaga en una solicitud útil.', icon: '✨' },
  { id: 'iterar', title: 'Aprende a iterar', description: 'Mejora una respuesta paso a paso.', icon: '🔁' },
  { id: 'problemas', title: 'IA para resolver problemas reales', description: 'Aplica IA a tareas cotidianas.', icon: '🛠️' },
  { id: 'verificar', title: 'Verifica lo que la IA te dice', description: 'Usa pensamiento crítico y fuentes.', icon: '🔎' },
  { id: 'reto', title: 'Reto final', description: 'Demuestra lo aprendido con un caso real.', icon: '🏁' }
];

const defaultState = { xp: 0, completed: [], awards: {}, exerciseText: '', lastScreen: 'home' };
let state = { ...defaultState, ...JSON.parse(localStorage.getItem('aprendeIA.progress') || '{}') };
const app = document.querySelector('#app');
const save = () => localStorage.setItem('aprendeIA.progress', JSON.stringify(state));
const completedCount = () => state.completed.length;
const escapeHTML = (text) => text.replace(/[&<>'"]/g, c => ({ '&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;' }[c]));

function addXP(key, amount) { if (!state.awards[key]) { state.awards[key] = true; state.xp += amount; save(); } updateHeader(); }
function updateHeader() { document.querySelector('#header-xp').textContent = `${state.xp} XP`; }
function progress() { return Math.round((completedCount() / modules.length) * 100); }
function showHome() {
  state.lastScreen = 'home'; save();
  const p = progress(); const firstDone = state.completed.includes('ia-basica');
  app.innerHTML = `<section class="hero"><div class="hero-copy"><p class="eyebrow">CURSO PRÁCTICO · 20% TEORÍA · 80% PRÁCTICA</p><h1>Aprende a usar <span>IA</span> haciendo.</h1><p>Aprende a trabajar con Inteligencia Artificial haciendo, experimentando y resolviendo problemas reales.</p><div class="btn-row"><button class="button" id="continue">${firstDone ? 'Continuar aprendiendo →' : 'Comenzar el curso →'}</button><button class="button secondary" id="reset">Reiniciar progreso</button></div></div><aside class="progress-card"><div><h2>Tu progreso</h2><div class="big-number">${p}%</div><span class="muted">${completedCount()} de ${modules.length} módulos completados</span></div><div><div class="progress-track"><div class="progress-bar" style="width:${p}%"></div></div><span class="muted">⚡ ${state.xp} XP acumulados</span></div></aside></section><section class="stats"><article class="stat-card"><span>Módulos disponibles</span><strong>${modules.length}</strong></article><article class="stat-card"><span>Completados</span><strong>${completedCount()}</strong></article><article class="stat-card"><span>Insignias</span><strong>${state.completed.length ? '1' : '0'} / 1</strong></article></section><section><div class="section-head"><div><p class="eyebrow">TU RUTA</p><h2>Elige tu siguiente misión</h2></div><span class="pill">Guarda tu avance en este dispositivo</span></div><div class="modules">${modules.map((m, i) => `<article class="module-card ${m.available ? 'available' : 'locked'}"><div class="module-top"><div><span class="module-number">MÓDULO ${String(i+1).padStart(2,'0')}</span><h3>${m.icon} ${m.title}</h3><p>${m.description}</p></div><span class="status ${state.completed.includes(m.id) ? 'done' : ''}">${state.completed.includes(m.id) ? '✓ Completado' : m.available ? 'Disponible' : 'Próximamente'}</span></div>${m.available ? '<button class="button secondary open-module">Abrir módulo →</button>' : ''}</article>`).join('')}</div></section>`;
  document.querySelector('#continue').onclick = showLesson;
  document.querySelector('.open-module').onclick = showLesson;
  document.querySelector('#reset').onclick = () => { if (confirm('¿Quieres borrar tu progreso guardado en este dispositivo?')) { state = {...defaultState}; save(); updateHeader(); showHome(); } };
}
function showLesson() {
  state.lastScreen = 'lesson'; save();
  const quizDone = !!state.awards.quiz; const missionDone = !!state.awards.mission; const complete = quizDone && missionDone;
  app.innerHTML = `<div class="lesson-wrap"><nav class="lesson-nav"><button class="button secondary" id="back">← Ruta del curso</button><span class="lesson-progress">MÓDULO 01 / 07</span></nav><section class="lesson-hero card"><p class="eyebrow">🧠 MÓDULO 01</p><h1>¿Qué es la IA?</h1><p>La IA es una herramienta que reconoce patrones y genera resultados a partir de instrucciones. No es magia: su calidad depende de cómo la uses y de cómo verifiques sus respuestas.</p><div class="progress-track"><div class="progress-bar" style="width:${complete ? 100 : quizDone || missionDone ? 55 : 12}%"></div></div><span class="muted">${complete ? 'Módulo completado' : 'Completa las dos actividades para terminar el módulo'}</span></section><section class="content-card card"><p class="eyebrow">20% TEORÍA</p><h2>Piensa en la IA como un copiloto</h2><div class="concept-grid"><div class="concept">🎯<b>Recibe instrucciones</b><span class="muted">Necesita contexto para ayudarte bien.</span></div><div class="concept">⚙️<b>Propone resultados</b><span class="muted">Puede redactar, resumir o generar ideas.</span></div><div class="concept">🔎<b>No siempre acierta</b><span class="muted">Tu criterio y verificación importan.</span></div></div></section><section class="content-card card" id="quiz-card"><p class="eyebrow">80% PRÁCTICA · +100 XP</p><h2>Mini quiz: ¿cómo aprovecharías la IA?</h2><p>Elige la mejor forma de pedir ayuda para preparar una reunión.</p><div id="quiz-options"><button class="quiz-option" data-answer="0">“Haz algo sobre la reunión.”</button><button class="quiz-option" data-answer="1">“No importa, decide tú todo.”</button><button class="quiz-option" data-answer="2">“Ayúdame a crear una agenda de 30 minutos para una reunión con mi equipo de ventas; incluye objetivos y próximos pasos.”</button></div><div id="quiz-feedback"></div></section><section class="content-card card"><div class="mission-header"><span class="mission-icon">🎯</span><div><p class="eyebrow">MISIÓN · +120 XP</p><h2>Mejora este prompt</h2></div></div><p>Prompt inicial:</p><div class="prompt-box">“Hazme un correo para mi jefe.”</div><p>Una petición útil da a la IA la información necesaria. Incluye cuantos criterios puedas:</p><div class="criteria" id="criteria">${['Contexto','Objetivo','Audiencia','Tono','Restricciones','Formato esperado'].map(x=>`<span class="criterion" data-c="${x}">${x}</span>`).join('')}</div><label class="muted" for="prompt-input">Escribe una versión mejorada (ejemplo: contexto, propósito, tono y formato).</label><textarea class="exercise-input" id="prompt-input" placeholder="Ejemplo: Soy analista de marketing. Redacta un correo breve para mi jefe, Ana, solicitando...">${escapeHTML(state.exerciseText)}</textarea><div class="btn-row" style="margin-top:14px"><button class="button" id="evaluate">Evaluar mi prompt</button><button class="button secondary" id="copy">📋 COPIAR PROMPT</button><a class="button secondary" target="_blank" rel="noopener" href="https://chatgpt.com/">🤖 ABRIR CHATGPT</a></div><div id="mission-feedback"></div></section>${complete ? '<section class="achievement">🏅 <strong>Insignia desbloqueada: Primera misión IA.</strong> Ya sabes que una buena instrucción incluye información útil y que tú validas el resultado.</section>' : ''}</div>`;
  document.querySelector('#back').onclick = showHome;
  document.querySelectorAll('.quiz-option').forEach(b => b.onclick = () => answerQuiz(Number(b.dataset.answer)));
  document.querySelector('#prompt-input').oninput = e => { state.exerciseText=e.target.value; save(); highlightCriteria(e.target.value); };
  document.querySelector('#evaluate').onclick = evaluateMission;
  document.querySelector('#copy').onclick = copyPrompt;
  highlightCriteria(state.exerciseText);
  if (quizDone) document.querySelector('#quiz-feedback').innerHTML = '<div class="feedback">✓ Quiz completado. Ganaste 100 XP.</div>';
}
function answerQuiz(answer) { const options = document.querySelectorAll('.quiz-option'); options.forEach((o,i)=>{o.disabled=true;if(i===2)o.classList.add('correct');if(i===answer && i!==2)o.classList.add('incorrect')}); const correct=answer===2; if(correct)addXP('quiz',100); document.querySelector('#quiz-feedback').innerHTML=`<div class="feedback">${correct?'¡Correcto! +100 XP.':'Casi. La mejor instrucción explica la tarea, el contexto y el resultado esperado.'} Una IA trabaja mucho mejor cuando sabe para quién es, qué debe hacer y con qué límites.</div>`; }
function criteriaFor(text) { const t=text.toLowerCase(); return { 'Contexto':/(soy|trabajo|empresa|proyecto|situación|porque)/.test(t), 'Objetivo':/(objetivo|quiero|solicitar|pedir|informar|comunicar)/.test(t), 'Audiencia':/(jefe|ana|gerente|director|equipo|cliente)/.test(t), 'Tono':/(tono|formal|cordial|amable|profesional|cercano)/.test(t), 'Restricciones':/(breve|máximo|menos de|sin |no |límite)/.test(t), 'Formato esperado':/(correo|asunto|párrafo|viñetas|formato|estructura)/.test(t) }; }
function highlightCriteria(text) { const found=criteriaFor(text); document.querySelectorAll('.criterion').forEach(el=>el.classList.toggle('met',found[el.dataset.c])); }
function evaluateMission(){const text=document.querySelector('#prompt-input').value.trim();if(!text){document.querySelector('#mission-feedback').innerHTML='<div class="feedback">Escribe tu propuesta antes de evaluarla.</div>';return}const hits=Object.values(criteriaFor(text)).filter(Boolean).length;const points=Math.max(20,hits*20);if(hits>=3)addXP('mission',120);document.querySelector('#mission-feedback').innerHTML=`<div class="feedback"><span class="score">${hits}/6 criterios detectados</span> · ${hits>=3?'¡Misión completada! +120 XP.':'Buen inicio: agrega al menos 3 criterios para completar la misión.'}<br>Tu prompt cubre ${hits} elementos. ${hits<6?'Prueba añadir los criterios que aún no están en verde.':'¡Excelente nivel de detalle!'}</div>`;if(hits>=3)setTimeout(showLesson,800)}
async function copyPrompt(){const text=document.querySelector('#prompt-input').value || 'Soy [rol/contexto]. Redacta un correo para [mi jefe/nombre] con el objetivo de [objetivo]. Usa un tono [tono], máximo [extensión] y entrégalo con [formato esperado].';try{await navigator.clipboard.writeText(text);document.querySelector('#copy').textContent='✓ PROMPT COPIADO';setTimeout(()=>document.querySelector('#copy').textContent='📋 COPIAR PROMPT',1800)}catch{document.querySelector('#mission-feedback').innerHTML='<div class="feedback">No se pudo copiar automáticamente. Selecciona el texto y cópialo manualmente.</div>'}}
updateHeader();
location.hash === '#modulo-1' || state.lastScreen === 'lesson' ? showLesson() : showHome();
